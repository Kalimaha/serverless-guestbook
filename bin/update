#!/usr/bin/env bash
aws cloudformation update-stack --stack-name serverless-guestbook \
                                --template-body file://stack.yml \
                                --region ap-southeast-2 \
                                --capabilities CAPABILITY_IAM
