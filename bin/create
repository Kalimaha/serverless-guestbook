#!/usr/bin/env bash
aws cloudformation create-stack --stack-name serverless-guestbook \
                                --template-body file://stack.yml \
                                --region ap-southeast-2 \
                                --capabilities CAPABILITY_IAM
